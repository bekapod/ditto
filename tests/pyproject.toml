[project]
name = "ditto-tests"
version = "0.1.0"
description = "PyBoy smoke tests for the ditto ROM"
requires-python = ">=3.10"
dependencies = [
    "pyboy>=2.7",
    "pytest>=8",
]

[tool.pytest.ini_options]
addopts = "-q"
